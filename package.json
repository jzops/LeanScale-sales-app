{
  "name": "leanscale-sales-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node data/parse-diagnostic-config.js && next dev -H 0.0.0.0 -p 5000",
    "build": "node data/parse-diagnostic-config.js && next build",
    "start": "next start -H 0.0.0.0 -p 5000",
    "generate-diagnostic": "node data/parse-diagnostic-config.js"
  },
  "dependencies": {
    "next": "14.0.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "recharts": "^3.7.0"
  }
}
